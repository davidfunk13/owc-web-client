import { makeStyles } from "../../utils/makeStyles";

const useStyles = makeStyles({ name: "DrawerItem Styles" })((theme) => ({
    drawerItem: { textAlign: "center" }
}
));

export default useStyles;