
import { makeStyles } from "../../utils/makeStyles";

const useStyles = makeStyles({ name: "UserMenu Styles" })((theme) => ({
    position: {
        marginTop: theme.spacing(5)
    }
}
));

export default useStyles;