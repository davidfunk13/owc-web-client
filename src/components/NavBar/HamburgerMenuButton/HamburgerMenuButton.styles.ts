import { makeStyles } from "../../../utils/makeStyles";

const useStyles = makeStyles({ name: "HamburgerIcon Styles" })((theme) => ({
    hamburgerIcon: {
        marginRight: 2,
    },
}));

export default useStyles;